# elo 打卡 App 安装使用说明（v1.0.4）

> 这份说明可以直接复制发给需要安装 App 的人。

---

## 一、这是什么

elo 打卡 是一个个人习惯打卡 App：给自己设定每天/每周要坚持的小目标，到点提醒、记录打卡、看坚持天数。数据存在云端服务器，换手机不丢。

## 二、怎么安装

1. 用手机浏览器打开下面链接（不要用电脑下载再转，直接手机点最方便）：

   `https://github.com/hywwz/elo-checkin/releases/download/v1.0.4/elo-checkin-v1.0.4.apk`

2. 如果打不开或下载太慢，也可以直接收我发的 APK 文件。
3. 下载完成后点文件安装：
   - 系统提示「是否允许安装未知来源应用」→ 点「允许 / 继续安装」；
   - 如果系统还要你打开「安装未知应用」开关，按提示打开后再点文件安装。
4. 安装完成后打开「elo 打卡」。

> 提示：如果手机显示“已安装旧版本但签名不一致”，先卸载旧版再装新版（数据在云端，卸载不丢记录）。

## 三、第一次使用

1. 打开 App 后注册账号（或用管理员给你的账号登录）；
2. 登录后点击「＋ 新目标」，设置目标名称、具体任务、打卡频率和提醒时间；
3. 打卡主页每天点「✓」完成打卡即可。

## 四、想让提醒准时弹出（必看）

App 的提醒是手机本地系统通知，需要给权限：

### 1. 通知权限

- 安卓：设置 → 应用 → elo 打卡 → 通知 → 打开「允许通知」
- 同时建议打开「悬浮通知 / 横幅」和「声音」，否则只在通知栏显示、不弹窗。

### 2. 精确闹钟权限（想要“到点准时报”）

- 设置 → 应用 → elo 打卡 → 特殊权限 / 闹钟和提醒 → 允许
- 或搜索系统设置里的「闹钟和提醒」，找到 elo 打卡并允许。

### 3. 小米 / 红米手机必须多做两步

- 设置 → 应用设置 → 应用管理 → elo 打卡 → 「自启动」打开；
- 同页面点「省电策略」→ 选择「无限制」。

不做这两步，小米会把 App 后台冻结，提醒不会自己弹出来（只有打开 App 才出现）。

## 五、三类提醒怎么设置

- 按时提醒：新建目标时设置的“提醒时间”，每天/每周按该时间提醒；
- 风险提醒：当天 22:00 还没打卡时提醒（可关闭）；
- 成就提醒：连续打卡满 7 / 30 / 100 / 365 天时推送庆祝（可关闭）。

开关位置：App 内「账号与安全」→「系统提醒」。

## 六、以后怎么升级

- 打开 App 会自动检查新版；也可以到「账号与安全」→「检查更新」手动检查；
- 发现新版本时点「立即更新」即可下载安装。

## 七、常见问题

| 问题 | 解决办法 |
| --- | --- |
| 点击安装没反应 | 换手机自带浏览器重新下载 |
| 安装时提示未知来源 | 在系统弹窗中允许该来源 |
| 有通知没声音/不弹横幅 | 检查通知权限里的声音与悬浮通知是否打开 |
| 小米手机后台不提醒 | 打开“自启动”并设置“省电策略=无限制” |
| 忘记密码 | 联系管理员重置 |

---

当前版本：v1.0.4（2026-09-08）
